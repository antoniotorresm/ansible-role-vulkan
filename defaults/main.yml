---
# possible options: all, amd, nvidia, intel
gpu_vendor: "all"

# Reboot is recommended,
# Vulkan might work incorrectly without restarting after install
reboot_on_install: true

# whether to install development libraries
install_devel: true
