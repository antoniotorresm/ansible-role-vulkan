---
gpu_vendor: "all" # possible options: all, amd, nvidia, intel

reboot_on_install: True # Vulkan might work incorrectly without restarting after install

install_devel: True # whether to install development libraries